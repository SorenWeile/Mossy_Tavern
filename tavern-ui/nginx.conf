server {
    listen 3000;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static files, falling back to index.html for React Router
    location / {
        try_files $uri $uri/ /index.html;
    }
}
